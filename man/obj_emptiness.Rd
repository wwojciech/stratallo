% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{obj_emptiness}
\alias{obj_emptiness}
\alias{is_empty}
\alias{is_nonempty}
\title{Object Emptiness (\code{NULL} or zero-length)}
\usage{
is_empty(x)

is_nonempty(x)
}
\arguments{
\item{x}{object to test.}
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#stable}{\figure{lifecycle-stable.svg}{options: alt='[Stable]'}}}{\strong{[Stable]}}

Utility functions for checking whether an object is empty, where emptiness
is defined as being \code{NULL} or having length 0.
}
\section{Functions}{
\itemize{
\item \code{is_empty()}: Returns \code{TRUE} if the object is \code{NULL} or has length 0, and \code{FALSE} otherwise.

\item \code{is_nonempty()}: Logical negation of \code{\link[=is_empty]{is_empty()}}.
This function directly checks if \code{length(x) > 0L} for performance reasons,
avoiding the extra negation step that would occur if using \code{!is_empty(x)}.
It is optimized for repeated use in algorithms where \code{\link[=is_nonempty]{is_nonempty()}} is called
many times.

}}
\examples{
# internal functions (not exported) – examples skipped

\dontrun{
is_empty(NULL)
is_empty(character(0))
is_empty(1)
}

\dontrun{
is_nonempty(NULL)
is_nonempty(character(0))
is_nonempty(1)
}

}
