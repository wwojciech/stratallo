% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_rdca.R
\name{check_rdca}
\alias{check_rdca}
\alias{rdca_obj_cnstr}
\alias{rdca_cnstr_check}
\alias{rdca_optcond_sU}
\title{Internal Diagnostic Functions for Checking Optimality of \code{rdca()}
Allocations}
\usage{
rdca_obj_cnstr(x, n, H_counts, N, S, rho2, J = integer(0))

rdca_cnstr_check(x, n, H_counts, N, S, rho2, J = integer(0), tol_max = 0.1)

rdca_optcond_sU(H_counts, S, rho, s, U, return_diff = FALSE)
}
\arguments{
\item{x}{(numeric)\cr vector of sample allocations.}

\item{n}{(\code{integerish(1)})\cr total sample size \eqn{n}. Must satisfy \verb{0 < n <= sum(N)}.}

\item{H_counts}{(\code{integerish})\cr strata counts in each domain.}

\item{N}{(\code{integerish})\cr strata sizes \eqn{(N_{d,h},\, (d,h) \in \mathcal H)}.}

\item{S}{(\code{numeric})\cr standard deviations \eqn{(S_{d,h},\, (d,h) \in \mathcal H)}
of surveyed variable in strata.}

\item{rho2}{(\code{numeric})\cr the square of \code{rho} (\code{rho^2}).}

\item{J}{(\code{integerish} or \code{NULL})\cr vector of domain indices, a subset of
\eqn{\mathcal D}. Specifies domains \eqn{d \in \mathcal D} for which
values of constraint functions \eqn{g_{dh}} are computed.
If \code{integer(0)}, these values are not computed. If \code{NULL}, all domains are
included.}

\item{tol_max}{(\code{integerish(1)})\cr the maximum tolerance exponent
(as a power of 10).}

\item{rho}{(\code{numeric})\cr parameters \eqn{(\rho_d,\, d \in \mathcal D)} of the
optimization problem.}

\item{s}{(\code{numeric})\cr vector of values for function
\eqn{s(\mathcal U, \boldsymbol{v}, d \mid p)} calculated only for domains
included in \code{U} that are not fully blocked by the take-max assignment.}

\item{U}{(\code{integerish} or \code{NULL})\cr a vector of indices identifying the
\emph{take-max} strata, i.e., the strata \eqn{(d,h)} for which the
allocation is fixed to \eqn{x_{d,h} = N_{d,h}}.
The indices refer to the positions of strata in the set \eqn{\mathcal H},
in the same order as in the input vectors (\code{N}, \code{S}, etc.).

For example, if \eqn{\mathcal H = \{(1,1),\, (2,1)\}} and
stratum \eqn{(2,1)} is a take-max stratum, then \code{U = 2}.

If \code{U} contains all strata from a domain, the dimension of the
D matrix is reduced accordingly.

\code{U} must satisfy one of the following conditions:
\itemize{
\item \code{n > sum(N[U])},
\item \code{n = sum(N[U])} and \code{n = sum(N)}.
}}

\item{return_diff}{(\code{logical(1)})\cr
If \code{FALSE}, the function returns a logical vector indicating whether the
optimality condition
\deqn{s(\mathcal{U},\, \boldsymbol{v},\, d \mid p) \ge \rho_d / S_{d,h}}
is satisfied for each unblocked stratum \eqn{(d,h) \in \mathcal U.}
If \code{TRUE}, the function returns the \strong{differences}
\deqn{s(\mathcal{U},\, \boldsymbol{v},\, d \mid p) - \rho_d / S_{d,h}}
instead of a logical vector, which can be used to assess by how much the
condition is satisfied or violated.}
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#stable}{\figure{lifecycle-stable.svg}{options: alt='[Stable]'}}}{\strong{[Stable]}}

Diagnostic tools to verify the objective function and constraint
satisfaction for allocations computed by the \code{\link[=rdca]{rdca()}} algorithm.
}
\section{Functions}{
\itemize{
\item \code{rdca_obj_cnstr()}: Compute the value of the objective function and constraint functions for a
given allocation.

\item \code{rdca_cnstr_check()}: Check whether the equality and inequality constraints are satisfied
for a given allocation, within a specified tolerance.
The tolerance applies to equality constraints only.

\item \code{rdca_optcond_sU()}: Check the optimality condition related to \eqn{s}.
Specifically, verifies whether
\eqn{s(\mathcal{U},\, \boldsymbol{v},\, d \mid p) \ge \rho_d / S_{d,h}}
for all \eqn{(d,h) \in \mathcal{U}} such that \eqn{d} is not fully blocked
by \eqn{\mathcal{U}}.

}}
\examples{
H_counts <- c(2, 2) # 2 domains with 2 strata each
N <- c(140, 110, 135, 190)
S <- sqrt(c(180, 20, 5, 4))
total <- c(2, 3)
kappa <- c(0.4, 0.6)
rho <- total * sqrt(kappa)
rho2 <- total^2 * kappa
n <- 500

(x <- dca(n, H_counts, N, S, rho, rho2))

# internal functions (not exported) – examples skipped

\dontrun{
rdca_obj_cnstr(x, n, H_counts, N, S, rho2)
rdca_obj_cnstr(x, n, H_counts, N, S, rho2, 2)
rdca_obj_cnstr(x, n, H_counts, N, S, rho2, NULL)
}

\dontrun{
rdca_cnstr_check(x, n, H_counts, N, S, rho2)
rdca_cnstr_check(x, n, H_counts, N, S, rho2, 1)
rdca_cnstr_check(x, n, H_counts, N, S, rho2, 2)
rdca_cnstr_check(x, n, H_counts, N, S, rho2, NULL)
}

\dontrun{
(n <- dca_nmax(H_counts, N, S) - 1)

U <- 1
(x <- dca(n, H_counts, N, S, rho, rho2, U = U, details = TRUE))
rdca_optcond_sU(H_counts, S, rho, x$s, U) # TRUE

U <- 2
(x <- dca(n, H_counts, N, S, rho, rho2, U = U, details = TRUE))
rdca_optcond_sU(H_counts, S, rho, x$s, U) # FALSE

U <- 3
(x <- dca(n, H_counts, N, S, rho, rho2, U = U, details = TRUE))
rdca_optcond_sU(H_counts, S, rho, x$s, U) # TRUE

U <- 1:2 # domain 2 blocked
(x <- dca(n, H_counts, N, S, rho, rho2, U = U, details = TRUE))
rdca_optcond_sU(H_counts, S, rho, x$s, U) # no unblocked strata in `U`
}

}
