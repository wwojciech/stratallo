% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_dca_rdca.R
\name{helpers_dca_rdca}
\alias{helpers_dca_rdca}
\alias{H_cnt2dind}
\alias{H_cnt2glbidx}
\alias{H_get_strata_indices}
\title{Internal Helper Functions for \code{dca0()}, \code{dca()}, and \code{rdca()}}
\usage{
H_cnt2dind(H_counts)

H_cnt2glbidx(H_counts)

H_get_strata_indices(H_counts, d)
}
\arguments{
\item{H_counts}{(\code{integerish})\cr strata counts in each domain.}

\item{d}{(\code{integerish(1)}) domain index. Must satisfy
\verb{0 < d <= length(H_counts)}.}
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#stable}{\figure{lifecycle-stable.svg}{options: alt='[Stable]'}}}{\strong{[Stable]}}

Internal utility functions used by \code{\link[=dca0]{dca0()}}, \code{\link[=dca]{dca()}}, and \code{\link[=rdca]{rdca()}} that
perform operations on sets of domain–strata indices and manage the mapping
between strata and domains.
}
\section{Functions}{
\itemize{
\item \code{H_cnt2dind()}: Creates a vector of domain indicators from a vector of strata counts per
domain;
each element of the vector is the index of the domain to which the
corresponding stratum belongs.

\item \code{H_cnt2glbidx()}: Creates unique indices for strata across multiple domains.
Returns a \code{list} of integer vectors, where the \eqn{d}-th element contains
the unique indices of the strata in domain \eqn{d}.

\item \code{H_get_strata_indices()}: Get the globally unique indices of strata belonging to a specific domain.

}}
\note{
These functions are internal and should typically not be called
directly by users.
}
\examples{
H_counts <- c(2, 2, 3) # three domains with 2, 2, and 3 strata respectively

# internal functions (not exported) – examples skipped
\dontrun{
H_cnt2dind(H_counts) # 1 1 2 2 3 3 3
}

# internal functions (not exported) – examples skipped
\dontrun{
H_cnt2glbidx(H_counts)
}

# internal functions (not exported) – examples skipped
\dontrun{
H_get_strata_indices(H_counts, 3) # 5 6 7
}

}
